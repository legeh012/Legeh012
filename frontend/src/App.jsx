import React from 'react';

export default function App() {
  return (
    <div style={{fontFamily: 'sans-serif', padding: 24}}>
      <h1>StoryForge (minimal)</h1>
      <p>This is a minimal frontend entry so the build can complete. The full app files remain in the repository root `src/` folder.</p>
      <p>To restore the original UI, move the app sources into `frontend/src/` or reconfigure the build.</p>
    </div>
  );
}
